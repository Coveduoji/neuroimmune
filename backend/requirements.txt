# Web 工作台后端依赖（Python 3.11+）
# 核心判断逻辑在 prototype/ 里，只额外依赖 httpx；后端本身是 FastAPI 薄壳。
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
python-multipart>=0.0.9
matplotlib>=3.8
python-docx>=1.1
